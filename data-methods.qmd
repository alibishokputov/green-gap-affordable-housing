---
title: "Data and methods"
subtitle: "Green infrastructure and affordable housing data behind the dashboard"
toc: true
toc-depth: 2
---

This page summarizes how each dataset was processed and what each dashboard
variable means. The visual overview below is a quick reference; the full
documentation follows it and is maintained as `references/data_and_methods.md`
in the repository.

Everything here is cross-sectional and descriptive. The measures show where
affordable housing and environmental conditions co-locate across census units.
They do not, on their own, establish a disparity between housing types or a
causal effect, because the data currently contain no comparison housing group.

## Data streams at a glance

| Stream | Source | Native resolution | Vintage | Dashboard variables |
|---|---|---|---|---|
| Tree canopy and land cover | Chesapeake Bay LULC, 2022 Edition | 1 m | ~2017 (DC), ~2018 (MD) | `canopy_pct`, `natural_canopy_pct` |
| Surface temperature and spectral indices | Landsat 8 and 9, Collection 2 Level-2 | 30 m (LST ~100 m effective) | Summer 2021 to 2023 | `mean_lst`, `mean_ndvi`, `mean_ndbi` |
| LIHTC affordable housing | HUD LIHTC database (LIHTCPUB) | point | Current inventory | `lihtc_units_low_income`, `lihtc_units_total`, `lihtc_projects` |

## Units of analysis

All variables are aggregated to U.S. Census areal units (2022 TIGER/Line). Two
geographies run in parallel so results can be compared across scale.

| Geography | Units (MD + DC) | Units with at least one LIHTC project | Role |
|---|---|---|---|
| Census block group | 4,650 | 741 | Default; finest unit carrying ACS data |
| Census tract | 1,681 | 545 | Coarser scale for sensitivity checks |

## Processing pipelines

How the three raw sources become the unit-level table the dashboard reads.

```{mermaid}
flowchart LR
  C["Chesapeake 1 m<br/>land cover"] --> Z["Zonal aggregation<br/>to census units"]
  L["Landsat 8 and 9<br/>summer composite"] --> Z
  H["HUD LIHTC<br/>inventory"] --> J["Spatial join<br/>to census units"]
  Z --> T["Unit-level<br/>analysis table"]
  J --> T
  T --> D["Dashboard<br/>bivariate map"]
```

### Tree canopy

Canopy combines the four canopy classes and is divided by classified land, so
open water and unclassified area (including military installations masked to
nodata) do not distort the share.

```{mermaid}
flowchart TD
  A["Chesapeake 1 m<br/>12-class land cover"] --> B["exactextract<br/>area-weighted class fractions"]
  B --> C["Canopy = classes<br/>3, 10, 11, 12"]
  B --> D["Land = classified area<br/>minus water"]
  C --> E["canopy_pct =<br/>canopy / land x 100"]
  D --> E
  E --> F{"Classified land<br/>vs Census land"}
  F -->|gap small| G["canopy_reliable = true"]
  F -->|gap large| H["canopy set to missing<br/>open water, military nodata"]
```

### Surface temperature and indices

Indices are computed for each scene, then reduced to a per-pixel median across
three summers. This order matters: the median is taken over the indices, not over
a composite of raw bands.

```{mermaid}
flowchart TD
  A["Landsat 8 and 9<br/>Collection 2 Level-2"] --> B["Filter: summer 2021 to 2023<br/>scene cloud below 60 percent"]
  B --> C["Mask cloud, shadow,<br/>cirrus, saturated pixels"]
  C --> D["Apply scale factors<br/>reflectance and temperature"]
  D --> E["Per scene: LST, NDVI,<br/>NDBI, NDWI, albedo"]
  E --> F["Median across<br/>3 summers, 30 m"]
  F --> G["Zonal mean per unit<br/>mean_lst, mean_ndvi, ..."]
```

### LIHTC affordable housing

```{mermaid}
flowchart TD
  A["HUD national LIHTC<br/>LIHTCPUB"] --> B["Restrict to MD and DC<br/>1,216 projects"]
  B --> C["Geocode; drop 77 without<br/>coordinates, 3 outside layer"]
  C --> D["Spatial join<br/>project point in census unit"]
  D --> E["Aggregate per unit<br/>low-income units, total units, projects"]
```

## Reading the bivariate grid

The dashboard sorts units into three classes on each axis and crosses them into a
3 by 3 grid. The corner cell marks co-location: the units with the most LIHTC and
the worst environmental value. Its position moves with the variable, since a low
value is worse for canopy but a high value is worse for temperature.

```{mermaid}
flowchart TD
  subgraph grid["3 x 3 bivariate classes"]
    direction TB
    R2["environment: high"]
    R1["environment: mid"]
    R0["environment: low"]
  end
  note["Corner cell = most LIHTC units<br/>+ worst environment.<br/>Low canopy, or high temperature."]
  R0 -.-> note
```

For canopy the worst class is low, for surface temperature it is high, so the
highlighted corner flips accordingly. The correlation reported in the dashboard is
an unadjusted rank correlation across units, shown without a significance marker
because at these sample sizes significance is uninformative.

# Housing type and environment (parcel-scale dashboard)

The bivariate dashboard above shows where LIHTC affordable housing and environmental
conditions co-locate across census units. It cannot compare affordable housing to
anything, because the areal data carry no other housing group. A second, parcel-scale
dashboard adds that comparison. It works at the **building** scale and sorts
multifamily rental stock into three types (subsidized, NOAH, and market-rate), then
contrasts them on the canopy and summer heat measured at each building's own
footprint.

Everything here remains cross-sectional and descriptive. A difference between NOAH
and market-rate buildings reflects where the two types sit, not an effect of the
housing type. Siting, building age, and neighborhood composition differ across the
types and none is adjusted for, so a gap is a statement about co-location, not a
disparity that housing type produced.

## What the three types mean

None of these categories comes from a registry. Market-rate is the residual, and
NOAH is derived from an affordability proxy, so the definitions are choices that a
reader should be able to see and contest.

| Type | Definition | Buildings |
|---|---|---|
| **Subsidized** | Footprint within 30 m of a HUD LIHTC record (trusted measure) | 595 |
| **NOAH** | Unsubsidized, assessed value/unit at or below the 60%-AMI cutoff | 2,419 |
| **Market-rate** | Unsubsidized, assessed value/unit above the cutoff | 1,988 |
| **Unknown** | Unsubsidized, no trustworthy unit count | 362 |

NOAH, naturally occurring affordable housing, is the policy-relevant category:
unsubsidized stock that rents affordably without a program and can be lost to the
market at any time. It has no subsidy record, so it is inferred from an affordability
threshold on the assessor's **value per unit**, anchored to Area Median Income (see
below), using public data only.

"Multifamily" here means purpose-built and small **rental** apartment stock.
Condominium and cooperative parcels are excluded: they are owner-occupied tenure,
recorded one parcel per unit rather than per building, so their per-unit values
would not be comparable and would swamp the apartment stock by count. One
consequence should be stated plainly. Investor-owned condominium units that are in
fact rented out are missed, because the assessor data cannot distinguish an
owner-occupied condo from a rented one without an ownership-occupancy join. The rental universe is
therefore the apartment stock, not every unit that happens to be rented.

## Housing-type data streams

| Stream | Source | Native resolution | Vintage | Dashboard variables |
|---|---|---|---|---|
| Parcels and assessed values | MD MdProperty View; DC Owner Polygons | parcel polygon | Feb 2026 (MD); 2026 snapshot (DC) | `value_per_unit`, `units`, `year_built` |
| Subsidized housing | HUD LIHTC (LIHTCPUB); NHPD national extract | point | Current inventory | `housing_type = subsidized` |
| Tree canopy and land cover | Chesapeake Bay LULC, 2022 Edition | 1 m | ~2017 (DC), ~2018 (MD) | `canopy_pct`, `natural_canopy_pct` |
| Surface temperature and indices | Landsat 8 and 9, Collection 2 Level-2 | 30 m (LST ~100 m effective) | Summer 2021 to 2023 | `mean_lst`, `mean_ndvi` |
| Flood exposure | FEMA National Flood Hazard Layer (SFHA) | polygon | NFHL, county effective dates 2006–2024 | `in_floodplain` |

## Unit of analysis: the building

The unit is the **multifamily building**, not the parcel and not the census area.

| | Parcels | Buildings | Multi-parcel buildings |
|---|---|---|---|
| MD (6 counties) + DC | 11,089 | 5,364 | 1,636 |

A single apartment complex is frequently split across several assessor parcels: one
parcel carries the value and units, and contiguous sub-parcels (garages, common
areas, phased buildings) report one unit or none. Left as parcels, roughly 2,200 of
the 11,089 report exactly one unit while carrying large values, which makes value per
unit meaningless. Aggregating to the building is the prerequisite for every
comparison the dashboard makes.

## Housing-type processing pipeline

How assessor parcels become the typed, environmentally-joined building table the
dashboard reads.

```{mermaid}
flowchart LR
  MD["MD MdProperty View<br/>parcels + assessed values"] --> P["Multifamily<br/>parcels (11,089)"]
  DC["DC Owner Polygons<br/>parcels + assessed values"] --> P
  P --> B["Dissolve to buildings<br/>by contiguity (5,364)"]
  B --> S["Flag subsidized<br/>LIHTC + NHPD within 30 m"]
  S --> N["Split unsubsidized by<br/>value/unit -> NOAH vs market"]
  N --> E["Extract canopy, LST, flood<br/>per footprint"]
  E --> D["Housing-type<br/>dashboard"]
```

### Building aggregation

Parcels that share a boundary within the same jurisdiction are treated as one
building; their values, units, and areas are summed. Owner is deliberately not used
to merge, because the only owner field available (a mailing address in Maryland, an
owner name in DC) is shared across unrelated properties held by the same management
company and would merge buildings that are not one structure.

```{mermaid}
flowchart TD
  A["Multifamily parcels<br/>11,089"] --> B["Queen contiguity<br/>within jurisdiction"]
  B --> C["Connected components<br/>= building groups"]
  C --> D["Sum value, units, area;<br/>max year built; mode use"]
  D --> E["5,364 buildings"]
  E --> F{"value/unit<br/>> $2,000,000?"}
  F -->|yes| G["Unit count untrustworthy<br/>-> type = unknown"]
  F -->|no| H["value/unit is<br/>the affordability axis"]
```

The value-per-unit ceiling catches buildings whose value and unit count sit on
separate parcels that did not merge, leaving an impossible per-unit figure. These
are set aside as `unknown` rather than counted as expensive market-rate stock.

### Subsidy flag: two measures, kept apart and not double-counted

A building is subsidized if its footprint sits within 30 m of an active subsidy
record. The two subsidy programs have different data quality, so they are carried as
**separate measures** rather than merged into one:

- **`lihtc`**, near a HUD LIHTC record. LIHTC is the trusted program and is the
  **operational subsidy flag**: it is what removes a building from the NOAH/market
  split. 595 buildings.
- **`nhpd_other`**, near an active NHPD record that is *not* LIHTC. NHPD adds
  coverage (project-based Section 8, public housing, Section 202/236, and others),
  but its data quality is weaker, so it is kept as a separate measure and not folded
  into the default. 255 buildings.
- **`subsidized_broad`**, the combined `lihtc` OR `nhpd_other` set, available for
  a sensitivity check. 778 buildings.
- **`section8`**, near an active project-based Section 8 property. Reported as its
  own flag. 246 buildings.

The de-duplication matters and is explicit. NHPD carries its own LIHTC flag; of the
1,606 active NHPD properties in the corridor, 814 *are* LIHTC. Those are removed from
`nhpd_other` before it is combined with the HUD LIHTC set, so a property that appears
in both sources is counted once, not twice.

Only currently **active** subsidies count. An NHPD property whose subsidy has expired
has lost it and belongs back in the unsubsidized pool.

```{mermaid}
flowchart TD
  A["HUD LIHTC<br/>MD + DC points"] --> L["lihtc flag<br/>(operational default)"]
  B["NHPD active, MD + DC"] --> D{"NHPD record<br/>is LIHTC?"}
  D -->|yes| X["drop (already in LIHTC),<br/>no double count"]
  D -->|no| N["nhpd_other flag"]
  L --> S["subsidized = lihtc"]
  L --> BR["subsidized_broad = lihtc OR nhpd_other"]
  N --> BR
```

Why LIHTC alone for the default: the subsidy flag decides which buildings are
excluded from NOAH, and a false positive there wrongly demotes an affordable building
out of the NOAH count. LIHTC is the program with the cleanest geocoded records;
leaning on NHPD for the default would import its data-quality problems into the core
label. NHPD is retained as the broad measure so the sensitivity of the NOAH count to
that choice is visible, not hidden.

### NOAH versus market-rate: an AMI-anchored cutoff

Among unsubsidized buildings, an affordability cutoff on assessed value per unit
separates NOAH from market-rate. The cutoff is not an arbitrary quantile of the
value distribution; it is anchored to an explicit affordability standard, a
household at a target Area Median Income (AMI) level, and every step of the
translation from that standard to a dollar figure is either sourced or measured.

```{mermaid}
flowchart TD
  A["HUD 80% AMI income limit<br/>3-person, FY2024"] --> B["Scale to target level<br/>(50 / 60 / 80% AMI)"]
  B --> C["Affordable rent =<br/>30% of income / 12"]
  C --> D["Affordable market value/unit =<br/>annual rent x GRM (8.5x, calibrated)"]
  D --> E["Affordable assessed value/unit =<br/>market value x assessment ratio"]
  E --> F{"building value/unit<br/>at or below cutoff?"}
  F -->|yes| G["NOAH"]
  F -->|no| H["market-rate"]
```

The chain, step by step:

1. **Affordable rent.** HUD's FY2024 Section 8 income limits give the 80% AMI (Low
   Income) limit for a 3-person household, a reference for a 2-bedroom unit. Lower
   AMI levels scale that limit. At 30% of income to rent, this fixes an affordable
   monthly rent (\$1,651/mo in the Maryland metros at 60% AMI).
2. **Rent to market value.** A gross rent multiplier converts affordable annual
   rent to an affordable *market* value per unit. Rather than assume a textbook GRM,
   it is **calibrated to the corridor**: the observed ratio of assessed value per
   unit to annualized ACS block-group median rent, divided by the per-state
   assessment ratio (step 3), implies a market GRM of ~7.9× in Maryland and ~9.2× in
   DC. The pipeline uses **8.5×**, the corridor mid-point. This is an approximation.
   ACS block-group median rent is a neighborhood rent across all rental stock, not
   the multifamily-specific rent of the building, and it is documented as such.
3. **Market to assessed value.** The parcels carry *assessed* value, which sits
   below market in both jurisdictions. The **assessment ratio** (assessed over
   sale price) is estimated directly from arms-length multifamily sales in the
   study parcels themselves (median 0.75 in Maryland from 1,586 sales, 0.93 in DC
   from 935), not borrowed from a statewide figure. Multiplying by it puts the
   cutoff on the same footing as the assessed values the buildings carry.

The AMI level is the one substantive lever, and three are stored so the label's
sensitivity is visible. **60% AMI is the default**, the income level LIHTC
units are targeted to, so NOAH is drawn at the same affordability line the
subsidized stock serves.

| Variant | AMI level | Maryland cutoff | DC cutoff |
|---|---|---|---|
| ami50 | 50% AMI | ~\$105k | ~\$130k |
| ami60 (default) | 60% AMI (LIHTC standard) | ~\$125k | ~\$156k |
| ami80 | 80% AMI | ~\$167k | ~\$208k |

This replaces an earlier per-state quantile cutoff, which drew the NOAH line at a
fixed fraction of each state's own value distribution. The quantile was
reproducible but arbitrary ("the cheapest 40%" is not an affordability standard),
and made NOAH mean something different in each state. The AMI-anchored cutoff is an
absolute, interpretable threshold: a NOAH building is one whose assessed value is
consistent with rents affordable at or below the target AMI. Assessed value remains
a proxy for rent (it lags market and is struck on staggered reassessment cycles),
so the label is only as sharp as the proxy and the GRM. That is why the AMI
variants and the measured assessment ratio are both carried explicitly.

### Does the value proxy actually track rent? A validation

The NOAH label is set from assessed *value*, but NOAH is a *rent* concept, so the
value proxy needs checking against observed rent. Building-level rent is not in
public data, but block-group rent is: ACS 2022 table B25063 gives the distribution
of gross rents by block group, from which the share of renter units paying at or
below the 60%-AMI affordable line (~\$1,650/mo) is computed for each block group.

The check is whether value-defined NOAH buildings sit where rents are actually
affordable. They do, clearly:

| Building type (value-defined) | Median % of BG renter units affordable at 60% AMI |
|---|---|
| NOAH | 75% |
| Subsidized (LIHTC) | 71% |
| Market-rate | 31% |

NOAH buildings sit in block groups where three-quarters of units rent affordably;
market-rate buildings sit where fewer than a third do. The value cutoff is picking
out genuinely affordable-rent locations, which is real corroboration that the proxy
is doing its intended job. Two honest caveats apply. This is a *block-group* rent,
not the building's own rent, so it validates location rather than the individual
unit. And ACS gross rent is contract rent for occupied units, which is not the
asking rent a newly listed unit would command. A building-level rent measure still requires listing data
(see `references/rent-based-noah-design.md`).

### Parcel-scale environment

The environmental measures are extracted for each building over the building's own
footprint, at the same definitions the areal pipeline uses for census units. The
building and the block group share the extractor, the rasters, and the canopy
arithmetic, so a building's canopy percent and a block group's canopy percent differ
only in the polygon supplied, not in how the number is computed.

**Area-weighted extraction, not centroid sampling.** Each footprint is intersected
with the raster using `exactextract`, which computes the exact polygon-by-pixel
overlap and weights every partially covered boundary pixel by its covered area. A
building is not represented by the single pixel under its centroid; it is the
area-weighted composite of every pixel its footprint touches. The median footprint is
about 2,675 m², large enough that a 1 m canopy raster and a 30 m thermal raster both
resolve it stably. The same extractor and the same `["unique", "frac"]` land-cover
call drive the areal canopy step, so the two scales are not just conceptually but
mechanically identical.

**Canopy arithmetic (shared with the areal pipeline).** The per-class area fractions
feed the same `derive_canopy_columns` routine documented under Tree canopy above.
`canopy_pct` is canopy (Chesapeake classes 3, 10, 11, 12, including canopy overhanging
structures and pavement) divided by classified land, with water and unclassified
nodata excluded from the denominator. `natural_canopy_pct` restricts the numerator to
class 3, canopy not over impervious surfaces, the more conservative green-space
reading. Summer surface temperature and NDVI are the area-weighted footprint means of
the 30 m Landsat summer-median stack (`mean_lst`, `mean_ndvi`). The Maryland and DC
1 m land-cover rasters are held separately and each building subset is reprojected to
its own raster's coordinate system before extraction; distances and buffers are in
metres (NAD83 Maryland State Plane, EPSG:26985).

```{mermaid}
flowchart TD
  A["Building footprint<br/>(median ~2,675 m²)"] --> B["exactextract<br/>area-weighted overlap"]
  A --> F["FEMA SFHA overlay<br/>(polygon intersection)"]
  B --> C["Chesapeake 1 m land cover<br/>-> canopy_pct, natural_canopy_pct"]
  B --> D["Landsat summer median 30 m<br/>-> mean_lst, mean_ndvi"]
  C --> E["Building environment table<br/>keyed on building_id"]
  D --> E
  F --> G["in_floodplain<br/>(1% floodplain, binary)"]
  G --> E
```

**Optional buffer.** A ring can be added around the footprint with `--buffer N`
(N metres) before extraction, so the measure reflects the building's immediate green
context rather than only its own roof and pavement. The buffer applies to the canopy
and Landsat extraction; it is not part of the cache key, so a buffered run is paired
with a forced recompute.

**Two honest differences from the areal measures.** First, the areal pipeline blanks
out canopy for units whose classified land falls well short of the Census land area
(open water, military installations masked to nodata), flagged as `canopy_reliable`.
The parcel pipeline carries no such gate: a building footprint sits on developed land
by construction, so the coverage problem that motivates the flag does not arise, but
the flag is simply absent rather than checked. Second, the areal Landsat step reports
a clear-observation count per unit as a reliability companion; the parcel step does
not. Neither difference changes a value, but both are worth stating for a reader
comparing the two tables.

The dashboard renders each building as a point at its centroid rather than as its
footprint polygon. The extraction is polygon-based and area-weighted; only the map
marker is a point, to keep the browser payload small.

**Flood exposure.** Each building carries `in_floodplain`, a binary flag set when its
footprint intersects the FEMA 1%-annual-chance floodplain (the Special Flood Hazard
Area, SFHA). The SFHA polygons come from FEMA's National Flood Hazard Layer, filtered
to the high-risk zones (A, AE, AH, AO, AR, A99, V, VE); the corridor holds 4,421 such
polygons and the overlay is a vector intersection of the footprint against them, not
a raster extraction. About 5% of multifamily buildings intersect the floodplain.

Three caveats belong with this variable. It is a coarse in-or-out flag on the
footprint: it records intersection with the 1% floodplain, not depth, base flood
elevation, or the share of the parcel exposed, so it should be read as "sits in the
mapped floodplain," not as a flood-risk score. The NFHL is effective-dated by county,
and across the study jurisdictions those dates span 2006 to 2024, so the flag inherits
a mix of map vintages that a comparison should acknowledge. And the flagged buildings
are few once split by housing type and state (on the order of 100 per type), so
type-by-type flood contrasts have little power and are reported as descriptive shares
rather than tested differences.

## Reading the housing-type dashboard

The dashboard reports **descriptive associations only**: medians, spatial
co-location, and correlations, no fitted model. Each building also carries ACS
block-group covariates (income, poverty, education, race/ethnicity) as
descriptors, but they are not entered into a regression here; the intent is an
honest picture of where each housing type sits and how the scales relate, not an
adjusted effect estimate. Five views, filterable by state, county (with an
unselect-all control), housing type, and environmental measure:

- **Building map.** A block-group choropleth of the chosen environmental measure, with
  buildings as points colored by housing type on top. LIHTC buildings are drawn as a
  distinct diamond marker, and buildings whose footprint intersects the FEMA
  floodplain carry a dark-blue outline ring, so the subsidized and flood-exposed stock
  are both visible against the rest. The sidebar carries a low-to-high value legend
  keyed to the choropleth's quantile breakpoints alongside the housing-type key.
- **Bivariate map (environment × LIHTC).** The areal view carried over from the first
  dashboard: block groups on a 3×3 grid crossing the environmental measure against
  LIHTC units, with a 3×3 legend showing the color for each environment-by-LIHTC
  combination. Block groups in the worst-environment tertile *and* the top LIHTC
  tertile, the "green-gap" co-location corner, are outlined in **red** and listed
  in a table below the map. This is spatial co-location, not evidence that LIHTC
  housing is treated worse.

Both maps carry the block group's census context in the hover tooltip: median
household income, poverty rate, share with a bachelor's degree or higher, the white
non-Hispanic, Black non-Hispanic, and Hispanic population shares, median gross rent,
and the share of renter units affordable at 60% AMI. The green-gap table lists the
same columns, so each flagged high-risk block group carries its socioeconomic profile
next to the environmental and LIHTC values. These are ACS 2022 5-year estimates;
block groups too small for the Census to publish a reliable figure show blank.
- **Type contrast.** The median environmental measure for each type, Maryland and DC
  shown separately (the type–environment relationship differs between them).
- **Type correlations.** A matrix of the correlation between each housing type and
  each environmental measure, at two scales, plus a strip plot of the selected measure
  by type. Discussed below.
- **Value × environment.** Value per unit against the environmental measure, with the
  NOAH cutoff drawn as a horizontal line.
- **Canopy paradox.** The building-scale and block-group-scale canopy results shown
  side by side and reconciled (below).

The headline value box reports the raw **difference in medians** between NOAH and
market-rate; a positive canopy value means NOAH sits at higher canopy. It is a
descriptive difference, not a correlation or an adjusted estimate.

## Correlating a categorical housing type with a continuous measure

Housing type is categorical, so "the correlation between housing type and canopy" is
not a single quantity. The Type correlations tab reports it in the two forms that are
defined, one per scale, and the methodology and its limits are worth stating in full
because the numbers are easy to over-read.

### The two estimators

At the **building scale** the measure is the **point-biserial correlation**. For a
given type, a binary indicator (1 if the building is of that type, 0 otherwise) is
correlated with the environmental measure across all multifamily buildings. The
point-biserial coefficient is algebraically a Pearson correlation with a 0/1 variable,
and it is monotonically related to the standardized difference in means between the
two groups. It therefore carries the sign of the between-type contrast: a positive
value means buildings of that type sit at higher values of the measure. NOAH and
canopy, for example, correlate at +0.10 (n = 5,364), the same direction as the median
contrast shown on the Type contrast tab.

At the **block-group scale** the measure is the **Spearman rank correlation between
the type's share of a block group's multifamily units and the block group's
environmental value**. Both quantities are continuous, so this is an ordinary
rank correlation rather than a point-biserial. It answers a different question:
across neighborhoods, does a higher concentration of this type coincide with higher
or lower environmental values. Spearman is used here (not Pearson) because the unit
shares are bounded and skewed and the block-group environmental distributions are
non-normal; the rank form is robust to both. Subsidized share and canopy correlate at
−0.11 (n = 1,736 block groups that contain multifamily units).

The two scales can disagree in sign. That is not an error; it is the canopy paradox
expressed as a correlation rather than a comparison of means, and showing both is the
point.

### What is and is not identified

Every coefficient in the tab is an **unconditional bivariate association**. It
conditions on nothing. A nonzero correlation reflects where each housing type is
located, not an effect of the housing type on its environment. The associations are
consistent with siting decisions, historical disinvestment, land-market sorting,
zoning, and post-development change, and the data cannot distinguish among these. The
tab is a descriptive instrument for Stages 1 and 2 of the analysis (distributional and
bivariate description); the conditional exposure difference the design targets, net of
parcel and neighborhood characteristics, is a separate estimand that these
correlations do not deliver.

### Caveats

- **Significance is uninformative at this sample size.** With roughly five thousand
  buildings, a correlation of 0.03 clears p &lt; 0.05. The stars in the matrix mark
  that threshold, but they should be ignored in favor of the sign and the magnitude.
  A coefficient near zero is near zero whether or not it is starred.
- **Multiple comparisons.** The matrix is 12 building-scale plus 12 block-group-scale
  coefficients, 24 tests in all, and no correction is applied. Read the matrix as a
  descriptive scan, not as 24 independent hypothesis tests; a single starred cell in
  isolation is weak evidence.
- **Group-size imbalance affects the point-biserial magnitude.** The point-biserial
  is attenuated when the two groups are far from equal in size. The types are
  unbalanced (NOAH 2,419, market-rate 1,988, subsidized 595), so the subsidized
  coefficients in particular are pulled toward zero by the small share of subsidized
  buildings, and their magnitude is not directly comparable to NOAH's. The sign
  remains interpretable.
- **Spatial dependence is not modeled.** Neither buildings nor block groups are
  independent observations; nearby units share canopy, heat, and housing patterns, so
  the effective sample size is smaller than the nominal n and the p-values are
  optimistic. This is a reason to distrust the stars, not the point estimates, and it
  is the same spatial-autocorrelation and MAUP concern that governs the areal analysis.
- **The block-group correlation is ecological.** It describes neighborhoods, not
  buildings, and cannot be read down to the individual building. Inferring a
  building-level relationship from the block-group coefficient would be an ecological
  fallacy, which is exactly the trap the two-scale presentation exists to prevent.
- **Redundancy among the measures.** Tree canopy, NDVI, and summer temperature are
  strongly intercorrelated, so the columns of the matrix are not independent readings;
  a type that correlates positively with canopy will tend to correlate positively with
  NDVI and negatively with temperature almost by construction.

A related correlation appears elsewhere in the documentation and on the first
dashboard: the block-group rank correlation between each environmental measure and
LIHTC *units* (rather than a type's unit share). That figure and the Type correlations
tab's LIHTC/subsidized row describe the same underlying pattern from slightly
different angles and are consistent in sign.

## The canopy paradox: two scales, both correct

The clearest example of why the dashboard shows both scales is tree canopy, where the
building comparison and the block-group correlation point in opposite directions.
Neither is wrong; they answer different questions.

**Building scale.** Affordable buildings are greener than market-rate multifamily:
median canopy is 16% for LIHTC/subsidized buildings, 19% for NOAH, and 12% for
market-rate. The affordable stock is not the least-green multifamily.

**Block-group scale.** Across block groups, canopy correlates *negatively* with LIHTC
units (Spearman −0.16, n≈4,600). More LIHTC in a block group goes with less canopy.

**The reconciliation is siting.** LIHTC concentrates in denser, more urban block
groups that are lower-canopy overall. Block groups with any LIHTC average 31% canopy,
those with none average 38%. So *across* neighborhoods, more affordable housing tracks
less canopy (a statement about which neighborhoods get LIHTC). But *within* those
neighborhoods, the affordable building's own parcel is not the least-green one (a
statement about buildings). This is an ecological, or modifiable-areal-unit, effect:
a block-group correlation and a building comparison can legitimately disagree, and
collapsing either into "affordable housing is greener/less green" would mislead.

Both statements matter for the study, and they say different things:

- The negative block-group correlation says **affordable housing is concentrated in
  the less-green parts of the region**, an environmental-justice observation about
  where affordable housing is built.
- The positive building comparison says **among multifamily, the affordable buildings
  are not themselves the least-green**, a within-neighborhood observation.

Among block groups that *have* LIHTC, the correlation nearly vanishes (−0.04), which
confirms the negative signal is almost entirely between-neighborhood sorting, not a
gradient within LIHTC neighborhoods.

---

*The complete documentation follows.*

{{< include references/data_and_methods.md >}}
