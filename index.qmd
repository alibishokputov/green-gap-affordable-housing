---
title: "Green Gap Affordable Housing"
subtitle: "Spatial analysis of the green gap in affordable housing — DC & Maryland"
---

## About this project

This site publishes the analysis notebooks for the **Green Gap Affordable Housing**
project: a spatial investigation of how green space (from land cover / land use rasters)
relates to affordable-housing locations (LIHTC and NHPD) across Washington, DC and Maryland.

The analysis combines:

- **Parcels** — February 2026 parcel geometries
- **Land cover / land use** — DC & MD raster editions (2017–2022 / 2018–2022)
- **Affordable housing** — LIHTC and NHPD (National Housing Preservation Database)

## Interactive dashboard

**[→ Green Gap dashboard](dashboard/)** — an interactive bivariate map of **tree canopy % ×
LIHTC affordable units** across 1,666 census tracts in Maryland and Washington, DC.

Canopy comes from the 1 m Chesapeake Bay land cover (all four tree-canopy classes, as a share
of classified land area); affordable housing comes from HUD's national LIHTC database. Filter
by jurisdiction, switch the canopy/LIHTC measures and classification scheme, and read off the
**green-gap tracts** — the least-green places carrying the most affordable housing.

The dashboard runs entirely in your browser (WebAssembly), so there is no server and nothing to
install.

## Notebooks

- [1.0 — Green gap demo](notebooks/1.0-example-green-gap-demo.ipynb):
  a worked example showing a `plotnine` chart and an interactive **bivariate choropleth** map.

## Reproducing the analysis

```bash
# install the environment (uv)
uv sync

# launch jupyter lab
uv run jupyter lab

# render this site locally
uv run quarto preview
```

## Data

Raw data lives under `data/raw/` (study inputs) and `data/external/` (third-party rasters), and
is **not** committed to the repository (see `.gitignore`). The one exception is
`app/tracts.geojson`, the small derived table the dashboard reads — CI cannot rebuild it,
since that needs the 2.4 GB land-cover rasters. See `references/` for provenance and data
dictionaries.
